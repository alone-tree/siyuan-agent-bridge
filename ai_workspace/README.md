# AI Workspace

This private folder is for agent-generated task context, analysis notes, drafts, and outputs. It is not synchronized back into SiYuan by this tool.

每次调用 siyuan_start 时，此文件夹下的所有内容（除本 README.md）都会被清理。siyuan_refresh_index 和 siyuan_create_document 不会清理此文件夹，以保留当前会话的工作文件。
